// Handle Flash USDT Form Submission
document.getElementById('usdtForm').addEventListener('submit', function (e) {
  e.preventDefault();
  const amount = document.getElementById('amount').value;
  const duration = document.getElementById('duration').value;

  if (amount && duration) {
    alert(`Flash USDT of ${amount} for ${duration} days generated successfully!`);
  } else {
    alert('Please fill all fields.');
  }
});

// Handle Buy Button Click
document.getElementById('buyButton').addEventListener('click', function () {
  const address = '0x00fC876d03172279E04CC30E5edCE103c3d23C1A';
  const amount = 0.01; // Test amount in USD
  alert(`Redirecting to wallet for payment of $${amount} to address ${address}`);
  // Add integration with wallets (e.g., MetaMask)
});
